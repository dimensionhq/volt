/// <reference types="react" />
import { ProviderProps } from '../index';
declare type Provider = (props: ProviderProps) => JSX.Element;
interface ProviderObj {
    provider: Provider;
    args?: any;
}
interface Props {
    providers: (Provider | ProviderObj)[];
    children: any;
}
declare const CombinedProviders: ({ providers, children }: Props) => any;
export default CombinedProviders;
